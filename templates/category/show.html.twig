{% extends 'base.html.twig' %}

{% block title %}Equipements dans {{ category.name }}{% endblock %}

{% block body %}
    <div class="container mx-auto p-6">
    {# en français #}
        <h1 class="text-3xl font-bold mb-6">Equipements dans {{ category.name }}</h1>
        
        <div class="grid grid-cols-1 sm:grid-cols-2 md:grid-cols-3 lg:grid-cols-4 gap-6">
            {% for equipment in equipments %}
                <div class="bg-white rounded-lg shadow-lg p-6">
                    {% if equipment.images|length > 0 %}
                        <img class="w-full h-48 object-cover mb-4" src="{{ asset('uploads/equipments/mini/300x300-' ~ equipment.images[0].name) }}" alt="{{ equipment.name }} - {{ equipment.images[0].name }}" loading="lazy">
                    {% endif %}
                    <h2 class="text-xl font-semibold mb-2">{{ equipment.name }}</h2>
                    <p class="text-gray-700 mb-2">Marque : {{ equipment.brand }}</p>
                    <p class="text-gray-700 mb-2">N° Série: {{ equipment.serialNumber }}</p>
                    <p class="text-gray-700 mb-2">Catégorie: {{ equipment.category.name }}</p>
                    <p class="text-gray-700 mb-2">Quantité: {{ equipment.stockQuantity }}</p>
                    <p class="text-gray-700 mb-2">Prix: ${{ equipment.price }}</p>
                    <p class="text-gray-700 mb-2">Statut: {{ equipment.status }}</p>
                </div>
            {% endfor %}
        </div>
    </div>
{% endblock %}
