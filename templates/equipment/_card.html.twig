<div class="bg-{{ bgColor }}-50 rounded-lg p-4">
    <div class="flex items-center">
        <i class="fas {{ icon }} text-{{ textColor }}-500 text-2xl"></i>
        <div class="ml-4">
            <h3 class="text-sm font-medium text-{{ textColor }}-800">{{ title }}</h3>
            <p class="text-2xl font-semibold text-{{ textColor }}-900">{{ count }}</p>
        </div>
    </div>
</div>
